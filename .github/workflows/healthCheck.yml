





name: Server health check

on:
  push:
    branches:
      - main

jobs:
  healthCheck:
    steps:
      - name: Check the deployed service URL
        uses: jtalk/url-health-check-action@v4
        with:
          # Check the following URLs one by one sequentially
          url: https://full-stack-open-pokedex-gb2w.onrender.com/




